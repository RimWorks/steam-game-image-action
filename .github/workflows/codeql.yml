name: codeql

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '17 6 * * 1'

permissions:
  contents: read

jobs:
  analyze:
    uses: RimWorks/mod-ci/.github/workflows/codeql.yml@d6261858b6b74c0cd3ca03c6402c954d58ac7fee # v1.5.1
    permissions:
      contents: read
      security-events: write
